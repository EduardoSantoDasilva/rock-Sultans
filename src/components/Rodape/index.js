import styles from "./Rodape.module.css"

function Rodape(){
    return(
        <footer className={styles.container}>
            <h3 className={styles.titulo}>Desenvolvido por Eduardo</h3>
        </footer>
    )
}

export default Rodape;